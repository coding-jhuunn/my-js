function performAdd(n) {
  let sum = (n * (n + 1)) / 2;
  return sum;
}
// the input size doesn't matter, it always be completed the exact amout of steps
// in the example it should be 0(3), but it is in smaller operations it doesn't matter
